# Component Conversion — Master Plan (temporary; delete when migration completes)

Consolidated from four meticulous planning passes (CourseController, ScheduleController,
Modals, App-shell controls). Each unit below = one agent-written implementation +
Playwright verification + commit. Lowest-risk first. Established pattern: `DepartmentSidebar.svelte`
(reads `appState`/`uiState` runes + services directly; reuses global CSS; `mount()` into the
existing host element after clearing it).

## Cross-cutting decisions (locked)
- **New small runes to add as needed** (consistent with "no listener systems"):
  - `uiState.currentThemeId` — written by `ThemeManager.setTheme`; ThemeSelector + settings-menu read it (ThemeManager is otherwise not reactive).
  - `selectedCourseId` channel (in `uiState` or `courseListState.svelte.ts`) — CourseList sets, CourseDescription reads.
  - `appState.autoScheduleTermPrefs` (already referenced by tutorial) — AutoScheduleIntro reads.
  - schedule **preview** rune (`schedulePreview.svelte.ts`) — vanilla wizard writes, Svelte TermGrid reads (bridge until wizard converts).
  - optional `appState.autoScheduleGeneration` — orchestrator bumps; AutoScheduleControls reads (defer to that sub-phase).
- **Modals**: ONE declarative `ModalLayer.svelte` gated by the existing `uiState.openModals` rune (NOT imperative on-demand mount). Collapses `ModalService.modals` + `openModals` into one source of truth and simplifies tutorial reconstruction. `Modal.svelte` shell absorbs BaseModal/ModalService show/hide/animation.
- **App.svelte shell is LAST** — only after course list, schedule page, and modals are components. It collapses `index.html` to `<div id="app">` + `mount(App)` and absorbs `UIStateManager.applyPageEffects/applyViewEffects`.
- Preserve element **ids/selectors** the tutorial + SchedulePicker depend on: `#planner-tab #schedule-tab #filter-btn #schedule-picker-btn #undo-btn #redo-btn`, `[data-course-id]`, `.course-select-btn`.

---

## PHASE 9 — App-shell controls (header) — ✅ DONE (all 6, each Playwright-verified + committed)
9A UndoRedoButtons · 9B ViewToggle · 9C PageTabs · 9D ThemeSelector (+`uiState.currentThemeId` rune) · 9E FilterButtons · 9F SearchBar.
LESSON (9F): a filter-sync `$effect` that reads the local input `$state` to compare MUST wrap the read/assign in `untrack(...)` — otherwise every keystroke re-runs the effect and resets the input to the not-yet-debounced filter value (Playwright caught this; same untrack lesson as the `watch` bridge). Settings mobile menu still vanilla → folds into App.svelte (Phase 13).
Original per-unit notes:
Each is tiny and independently committable. Mount into existing hosts.
- **9A `UndoRedoButtons.svelte`** — host `.undo-redo-controls`. `$derived`(read `appState.undoRedoGeneration` then `psm.canUndo()/canRedo()`). Delete MainController 701-717, 736-742, `updateUndoRedoButtons` 1235-1246. Keep keydown or move to `<svelte:window>`.
- **9B `ViewToggle.svelte`** — host `#view-list`/`#view-grid` (wrap). `$derived(uiState.currentView)`; `class:` bindings replace `UIStateManager.applyViewEffects` (delete it).
- **9C `PageTabs.svelte`** — host `.nav-tabs-pill`. `$derived(uiState.currentPage)`; `onSwitch` callback keeps render side-effects. Trim tab `.active` out of `applyPageEffects` (keep page display toggle for now). Delete dead `mobile-menu-btn` lookups.
- **9D `ThemeSelector.svelte`** — host `.theme-selector`. Import `theme-selector.module.css`. Drive from new `uiState.currentThemeId` rune (fixes settings-menu-toggle desync + init timing). Delete `ThemeSelector.ts`.
- **9E `FilterButtons.svelte`** — filter/bookmark/clear (+schedule-filter deferred). `$derived` off `filterService.getActiveFilters()` (SvelteMap). Delete `updateFilterButtonState/updateClearFiltersButtonState/updateBookmarkFilterButtonState/updateScheduleFilterButtonState` + setTimeout init 160-164.
- **9F `SearchBar.svelte`** — host `.search-input-wrapper`. Keep `DebouncedOperation`. `$effect` syncs input from `searchText` filter (replaces `syncSearchInputFromFilters`). professorMode local `$state`.
- (Settings mobile menu → defer, fold into App.svelte.)

## PHASE 10 — CourseController → 3 sibling components  ✅ DONE (10A + 10B + 10C)
10C done: `CourseDescription.svelte` mounted into `#course-description`, props `{ courseDataService }`; reacts to `courseListState.selectedCourse`. `courseListState` now stores the `Course` OBJECT (`selectedCourse = $state.raw`) with a `get selectedCourseId()` getter (getter-through-$state.raw IS reactive — CourseList's `class:active` highlight verified still tracking). Both CourseList + SelectedCoursesPanel now ONLY set the rune (the `onSelectCourse` vanilla bridge prop is deleted from both). Ported displayCourseDescription/renderComponentTabs/populatePanel/renderSectionCard faithfully to declarative Svelte: tabs = `$derived` filtered list (lectures/discussions/labs/interest-lists, order preserved); active tab = local `$state` reset to first tab on course change via an `$effect` keyed on `course?.id` (writes activeTab inside `untrack`); panels render directly (no lazy `data-loaded`). `CATEGORY_DESCRIPTIONS` moved into the component. **DELETED: `CourseController.ts` + `ProgressiveRenderer.ts` + both index exports + MainController's `courseController` field/ctor/setFilterService/setAllDepartments calls.** Repo-wide grep for CourseController/ProgressiveRenderer/showCourseDescription/onSelectCourse is clean (only historical comments remain). Playwright-verified: list-click + panel-click both drive description, multi-tab switch (AE-2320 Lectures/Labs → "Available Labs (3)"), active-tab resets to first on course change, list `.active` highlight tracks selection, section cards intact, empty-state, 0 console errors. NOTE (cleanup for a later pass): MainController still has write-only dead members `previousSelectedCoursesCount` + `getAllCourses` + an unused `scheduleManagementService` (LSP hints, tsc clean — not blocking).
INCREMENTAL: CourseController.ts stays alive until 10C (still owns the description panel + `selectCourse`/`getCourseFromElement`/`showCourseDescription`). 10A done: `CourseList.svelte` + `courseListState.svelte.ts` (shared `selectedCourseId` rune). Clicking a course sets the rune AND calls `onSelectCourse(course)` → vanilla `CourseController.showCourseDescription` (bridge until 10C). Deleted the ~250-line term-expansion FLIP animation + list click branches + `displayCoursesWithCancellation`/`handleLoadMoreClick`; trimmed `refreshCurrentView` to schedule-only. LESSON (10A): `transition:slide` runs an intro on EVERY item's term container at mount (100+ items) → `NaNpx` height-keyframe console warnings (Playwright caught 52). Dropped slide → instant toggle (spec allowed it). A polished expand animation would need CSS, not JS slide. Cross-reactivity verified: select in list ↔ selected-panel both update via `appState.selectedById`.
10B done: `SelectedCoursesPanel.svelte` mounted into `.selected-courses-section` (cleared first). Renders whole section (header expander + list), preserves ids `selected-courses-header`/`-list`/`selected-count`/`-chevron`. `$derived` sorted copy of `appState.selectedCourses` (never mutate `$state.raw`); expander state in `localStorage('selectedCoursesExpanded')`. Item click → set `courseListState.selectedCourseId` + `onSelectCourse` bridge. Remove btn calls `e.stopPropagation()` (so the GLOBAL delegated `.course-remove-btn` handler — KEPT for the still-vanilla SCHEDULE sidebar — doesn't double-fire) then `unselectCourse`. Deleted from CourseController: `displaySelectedCourses`/`addSelectedCourseToSidebar`/`removeSelectedCourseFromSidebar`/`initializeSelectedCoursesExpander` (~172 lines). MainController: deleted the `.selected-course-item` click branch + the add/remove sidebar loops in `refreshSelectionUI` + `displaySelectedCourses()` calls; `syncCourseSelectionUI()` kept as a now-empty no-op (still called by setupTutorial). Playwright-verified: reactive add (sorted), item-click drives description, remove (no double-fire), empty-state, expander+localStorage persist, schedule sidebar untouched, 0 console errors.
Original notes:
- **10A `CourseList.svelte`** — host `#course-container`. `$derived` filtered+sorted+paginated list (relocate `refreshCurrentView`'s course computation). Term-badge expand = per-course local state + Svelte `transition` (DELETE the ~250-line FLIP animation in MainController). Keep `data-course-id`/`.course-select-btn`. Keep `displayCount` pagination.
- **10B `SelectedCoursesPanel.svelte`** — host `.selected-courses-section`. `$derived` sorted `appState.selectedCourses`; expander state in localStorage. `.course-remove-btn` uses `stopPropagation` (schedule sidebar still uses the MainController delegated handler).
- **10C `CourseDescription.svelte`** — host `#course-description`. Reads shared `selectedCourseId`; tabs lazy via `$derived`.
- Delete `CourseController.ts` + `ProgressiveRenderer.ts` + index export + all MainController dept/course render wiring. Trim (don't delete) `refreshCurrentView` to keep filter-button/search side-effects.

## PHASE 11 — Modals → declarative layer  (11A ✅ + 11B ✅ + 11C ✅ + 11D ✅ + 11E ✅ + 11F ✅ + 11G ✅ — PHASE COMPLETE)
**DEV-SERVER VERIFICATION RULE (learned 11C, reconfirmed 11D — costs ~15-20min each time ignored):** ModalLayer is mounted via `mount()` (not in an HMR component tree), so a RUNNING Vite dev server does NOT reliably pick up edits to ModalLayer/mounted Svelte components — the modal silently renders nothing (openModals + payload are correct, but `#modal-root` shows only empty `<!---->` anchors). ALWAYS `pkill -f vite`, start ONE fresh server, then full-reload before Playwright-verifying a newly-registered modal branch. Also confirm exactly one server owns :3000 (`ss -ltnp | grep :3000`) — a stale server from a prior session on `[::1]:3000` will shadow a new one on `127.0.0.1:3000` since `localhost` resolves to `::1`.
11B done: 4 leaf modals migrated — `Changelog.svelte` (id 'changelog', ports CHANGELOG.md?raw parse), `Tutorials.svelte` ('tutorials', needs `tutorial` service), `SectionInfo.svelte` ('section-info', payload), `DeleteLocalEvent.svelte` ('delete-local-event', payload). Created `src/svelte/modals/modalState.svelte.ts` (`$state.raw` payloads: `sectionInfo: SectionData | null`, `deleteLocalEvent: {title,onConfirm} | null`) — vanilla trigger sites set the payload THEN call `modalOpened(id)`. Relocated the `SectionData` interface from the deleted SectionInfoModalController.ts into `src/types/modal.ts`. Rewired triggers: SchedulePickerModal (still vanilla) changelog/tutorials buttons → `uiStateManager.modalOpened(...)`; ScheduleController (still vanilla) delete-event + section-info → set `modalState` + `modalOpened`. DELETED: ChangelogModal, TutorialsModal, DeleteLocalEventModal, SectionInfoModalController, InfoModalController (the last was constructed-then-discarded = dead) + their index exports. **BUG caught by Playwright (lazy-prop):** ModalLayer was mounted in MainController's ctor with `tutorial: services.tutorial`, but `services.tutorial = setupTutorial(...)` runs in main.ts AFTER `new MainController()` — so the static mount prop captured `undefined` and the Tutorials modal never rendered (Changelog, with no such dep, worked — which masked it). FIX: pass `getTutorial: () => services.tutorial` as a THUNK; ModalLayer reads it lazily via `{@const tutorial = getTutorial()}` inside the `{#each uiState.openModals}` (which re-runs when openModals changes, i.e. when tutorials opens, by which point it's set). LESSON: anything assigned to `services` AFTER MainController construction (tutorial, and anything set in main.ts post-ctor) must reach mounted Svelte components via a thunk/rune, never a static mount-time value. Playwright-verified all 4 (changelog 25 entries + Escape-close; tutorials 5 buttons + backdrop-close AFTER the thunk fix; section-info rendered with a real injected payload — title/professor/section/color input; payload modals null-safe when opened without payload), 0 console errors.
Batches, simplest-first, each committable. `Modal.svelte` + `ModalLayer.svelte` + `modalState.svelte.ts`; mount ModalLayer once (new `#modal-root`).
FOUNDATION (built in 11A, reused by all later batches): `src/svelte/modals/Modal.svelte` is the shell — props `{ typeId, closeOnBackdrop=true, closeOnEscape=true, title?, onRequestClose, children: Snippet<[()=>void]> }`. It reproduces ModalService's animation EXACTLY: `onMount` double-rAF sets `shown=true` (CSS `.modal-container.show`/`.modal-dialog.show`); `close()` sets `closing=true` (CSS `.hide`) then `setTimeout(onRequestClose, 200)` so the hide anim plays before unmount. Renders `.modal-backdrop.modal-container` > `.modal-dialog` (role=dialog, aria-modal, stopPropagation) > `.modal-content` > `{@render children(close)}` — concrete modals get the `close` fn via the snippet param. `<svelte:window onkeydown>` handles Escape (guarded `shown && !closing`). `ModalLayer.svelte` (props `{ uiStateManager }`, mounted ONCE into `#modal-root` by MainController ctor) reads `uiState.openModals` and `{#each}`-renders the component for each KNOWN id, IGNORING unknown ids — so vanilla modals (which also push their ids to openModals via `uiStateManager.modalOpened`) coexist untouched until migrated. Close path goes through `uiStateManager.modalClosed(id)` (keeps the rune + tutorial snapshots consistent). `modalState.svelte.ts` DEFERRED until the first batch with a payload (MobileNotice has none). KEY: `uiState`/services aren't on `window`, but `window.mainController.services.uiStateManager` is reachable for Playwright — drive `modalOpened(id)` (the real production trigger path) to test.
- **11A** ✅ MobileNotice. `MobileNotice.svelte` wraps Modal (closeOnBackdrop/Escape both false; only "Got it" closes). Trigger moved: `main.ts` mobile check now calls `services.uiStateManager.modalOpened('mobile-notice')` instead of `new MobileNoticeModal().show()`. Deleted `MobileNoticeModal.ts`. Playwright-verified full lifecycle (show anim, backdrop/Escape correctly ignored, "Got it" → hide anim → unmount, openModals ['mobile-notice']→[]), 0 console errors.
- **11B** leaf confirm/info: DeleteLocalEvent, Changelog, Tutorials, SectionInfo; delete dead `InfoModalController`.
- **11C** ✅ LocalEvent. `LocalEvent.svelte` (id 'local-event') ports the add/edit-event form to runes: `$state` for title/description/eventType/date/start/end + `selectedDays: Set<DayOfWeek>` + `selectedTerms: Set<string>`; `$effect` (guarded on payload identity via a `seededFor` ref) re-seeds all fields when a new payload arrives — handles both add (defaults: Monday + all terms + 09:00–10:00) and edit (`existingEvent`). Day pills keep ≥1 selected; per-field error flags drive `class:form-error` (title required, ≥1 day + ≥1 term for recurring, date for one-time, end>start). `{#if eventType===ONE_TIME}` swaps date picker ↔ day/term selectors. Imports the CSS module (`local-event-modal.module.css`) directly in `<script>` and uses `class={styles[...]}`; icons via `{@html getInlineSVG(...)}`. Added `localEvent: LocalEventPayload | null` (`{ onSave, existingEvent? }`) to `modalState.svelte.ts`. Rewired ScheduleController `openAddLocalEventModal()` from `new LocalEventModal(modalService,...).show()` to `modalState.localEvent = {onSave} ; uiStateManager.modalOpened('local-event')` (dropped the modalService guard — now only needs currentSchedule). DELETED `LocalEventModal.ts`. Playwright-verified BOTH modes end-to-end: defaults (5 day pills/Mon selected, 4 terms all checked), empty-title blocked, recurring save → payload days [M,T] terms [A,B,C] (D unchecked), one-time save → date field appears + recurring fields hide + end-before-start blocked + payload has date & no days/terms, modal closes after each save, 0 console errors. **GOTCHA (cost ~20min): a stale dev server from a prior session was still listening on `[::1]:3000` while my fresh one bound `127.0.0.1:3000`; the browser's `localhost` resolved to `::1` → hit the OLD bundle (no local-event branch) so the modal silently didn't render while changelog did. Fix: `pkill -f vite`, start ONE server, reload. LESSON: before Playwright-verifying a new modal/branch, confirm exactly one dev server owns :3000 (`ss -ltnp | grep :3000`).**
- **11D** ✅ AutoScheduleIntro + delete `ModalQueue`. `AutoScheduleIntro.svelte` (id 'auto-schedule-intro') ports the per-course term-picker grid to runes: `SvelteMap<courseId, SvelteSet<term>>` for selection (granular `.size`/`.has` reactivity), payload-identity-keyed `$effect` seeds it (sections-already-picked courses start EMPTY/excluded, others default to all available terms = union of `course.lectures[].section.computedTerm` + `standaloneLabs[].computedTerm`). Card click toggles all-available; term-badge click toggles one (stops card toggle); A/B/C/D badges render available(selectable)/unavailable. **Next** computes the filtered list (a course gets `allowedTerms:[...selected]` ONLY if not all its available terms are still selected, else passed unchanged) → calls `payload.onNext(filtered)` → `close()`. Added `Modal.svelte` `extraClass?` prop (backdrop class, here 'filter-modal' for width — 11F FilterModal will reuse). modalState gains `autoScheduleIntro: {selectedCourses, getColor, onNext} | null` + `autoScheduleIntroTermPrefs: Record<string,string[]> | null` (the tutorial override channel: a `$effect` merges it into the SvelteMap then nulls it — replaces the old imperative `setTermPreferences`). ScheduleController: `openAutoSchedule()` no longer builds a `ModalQueue` — it sets `modalState.autoScheduleIntro` (onNext → new `openScheduleFilterModal(filtered)` helper that builds+shows the still-vanilla FilterModalController) + `modalOpened('auto-schedule-intro')`; tutorial `openAutoScheduleIntro()` sets the payload (onNext → openScheduleFilterModal); `updateAutoScheduleIntroTerms(prefs)` now just sets `modalState.autoScheduleIntroTermPrefs`. Removed `currentAutoScheduleIntro` field. DELETED `ModalQueue.ts` + `AutoScheduleIntroModal.ts` + both index exports. Playwright-verified with 3 synthetic courses: correct seed defaults (incl. picked-course-excluded), card/term toggles, tutorial term-pref override merge + channel auto-clear, Next → correct `allowedTerms` narrowing + close, 0 console errors. (NOTE: required a fresh dev-server restart to render — see DEV-SERVER VERIFICATION RULE above.)
- **11E** ✅ SchedulePicker. `SchedulePicker.svelte` (id 'schedule-picker') ports the whole modal to runes: the schedule list is `$derived.by` reading `appState.activationGeneration` + `appState.selectedById` (the two runes the old `watch` bridges keyed on) plus a local `refreshTick` `$state` bumped after every list-MUTATING action (rename/duplicate/delete/import/create/clear) — that tick replaces the old imperative `updateScheduleList()` calls (switch + selection changes are covered by the runes automatically). Active-schedule highlight + per-item course count (active = live `getCourseSelectionService().getSelectedCourses().length`, else stored `schedule.selectedCourses.length`) are reactive. Inline rename = `editingId`/`editValue` `$state` swapping the name div for an `<input>` (`use:focusSelect` focuses+selects; Enter commits via blur, Escape cancels). The per-item ⋮ menu = `openMenuId` + `menuPos` `$state` (fixed-position popup computed from the button rect with the same viewport clamping; closed via `<svelte:window onclick>`). Horizontal page-slide tabs: local `activeTab` `$state` drives a `$effect` that sets `.modal-pages-container` `scrollLeft` (CSS `scroll-behavior:smooth` animates); `class:active` on the footer tabs. **DELETED the two `watch(...)` bridges** (the only consumer-side win of this unit) — the component reads the runes directly. Added `Modal.svelte` `dialogClass` prop (here `'schedule-picker-modal-dialog no-transform'` — the dialog-scoped CSS + opt-out of the scale animation). Tutorial tab-nav channel = new self-clearing `modalState.schedulePickerTab` (`navigateSchedulePickerToTab` sets it; a `$effect` applies it to `activeTab` then nulls it — replaces the imperative `navigateToTab`/double-rAF). `getTutorial` thunk gates the Tutorials settings button (same post-ctor-assignment reason as 11B). ALL settings-button ids preserved (`#new-schedule-btn-settings` etc. — tutorial step selectors) + `#tutorials-btn-settings`, plus the mobile header `+ New Schedule`. ModalLayer gains a `scheduleManagementService` prop (passed at mount). MainController: `openSchedulePicker()` → `uiStateManager.modalOpened('schedule-picker')`; `navigateSchedulePickerToTab()` → `modalState.schedulePickerTab = tab`; deleted the `schedulePickerModal` field + import. **DELETED `SchedulePickerModal.ts` + its index export.** Playwright-verified: list renders + active highlight, row-click switch (+ service pruning of the empty Tutorial schedule reflected reactively), create (add-btn, stubbed prompt), duplicate, inline rename (dblclick), delete (>1 schedule gate + stubbed confirm), ⋮ menu open/position/outside-close, Settings-tab slide + tutorial-channel nav, Changelog opens over picker (picker stays) + ×-close leaves picker, Tutorials button closes picker + opens tutorials, and ×/backdrop/Escape close. 0 app console errors (tsc: only the 2 pre-existing FilterModalController errors; build clean). NOTE: stacked Modal.svelte instances each own a `<svelte:window>` Escape handler, so Escape over the changelog also closes the picker underneath — same as the old vanilla ModalService Escape behavior, NOT a regression (proper stack-aware Escape can wait for 11G teardown).
- **11F** ✅ FilterModal (+`auto-schedule-filter` mode). The biggest unit, but it never used `watch` and its 8 filter sections are pure imperative DOM read/write against `filterService` — so rather than convert every checkbox to a rune (high-risk, no reactivity win), this collapses the modal into the declarative ModalLayer while KEEPING all the tested filter logic. **Refactored `FilterModalController` to stop extending `BaseModal`**: no-arg ctor, new `renderInto(container, requestClose)` (builds header+body+footer into the host Modal.svelte provides — the backdrop/dialog/`.modal-content`/animation now live in Modal.svelte) replacing `show()`/`createModalElement()`; `this.hide()` → `requestClose()`; added `destroy()` that tears down the RMP `DualRangeSlider`s' document-level listeners (the old BaseModal path LEAKED these on every open — now stored on `this.sliders` and destroyed on unmount). **Fixed the 2 long-standing tsc errors** (`syncSearchInputFromMain`/`syncMainSearchInput`/`updateClearSearchButton` referenced a `.search-text-input` that no longer exists — deleted them; "Clear All" actually THREW at runtime before this). `FilterModal.svelte` wraps `Modal.svelte` (`extraClass="filter-modal"`, new `dialogClass="filter-modal-dialog"` prop on Modal) with a `display:contents` host + a `use:mountPanel` action that constructs the controller from props (filterService/courseSelectionService/autoScheduleOrchestrator/profileStateManager + `getDepartments` thunk), reads `modalState.filter` (`{mode, onGenerate?, coursesToSchedule?}`) for mode + auto-schedule continuation, and calls `renderInto(host, close)`; action `destroy()` calls `controller.destroy()`. New `modalState.filter` payload + `modalState.filterRefreshTick` ($state) — a `$effect` re-runs `controller.refreshFilterUI()` on bump (replaces `refreshFilterUI`/`refreshAutoScheduleFilterUI` imperative calls). ModalLayer registers BOTH `filter-modal` and `auto-schedule-filter` → `<FilterModal>` (services passed as ModalLayer props from MainController mount). **Collapsed the TWO planner instances** (`filterModalController` + `scheduleFilterModalController`) into one declarative `filter-modal`: MainController `openFilterModal()` (planner filter btn + schedule-filter btn both) → `modalState.filter={mode:'filter'}; modalOpened('filter-modal')`; `refreshPlannerFilterUI()` → bump tick; `syncModalSearchInput` now a no-op (the old target never existed). ScheduleController `openScheduleFilterModal`/`openAutoScheduleFilter` → `modalState.filter={mode:'auto-schedule',coursesToSchedule,onGenerate}; modalOpened('auto-schedule-filter')` (deleted the ephemeral `FilterModalController` instances + the `Object.defineProperty(modalTypeId)` hack + `currentAutoScheduleFilter`); `refreshAutoScheduleFilterUI()` → bump tick. SharedFilterComponents/SharedFilterSetup/DualRangeSlider KEPT (still used by the refactored controller). Playwright-verified BOTH modes: planner modal renders 8 sections + dialog/backdrop ids + "Filter Courses"/"Apply"; department checkbox/select-all(70)/none/search/category-mode toggle, term/gradLevel/credit-quick/availability all write `filterService` + update preview + `#filter-count` badge; RMP `DualRangeSlider` keyboard-drag (ArrowRight → min 0.5, rmpRating activates); Clear All resets + re-renders 8 sections; course list reacts; schedule-filter btn opens same modal; auto-schedule mode shows "Auto-Schedule Settings"/"Generate Schedule"/preview, Generate → `doGenerateSchedules` + close; refresh-tick re-syncs checkboxes; close via ×/backdrop/Escape/Apply; openModals tracking. 0 app console errors. **GOTCHA (~15min): headless Playwright froze `requestAnimationFrame` mid-session, so Modal.svelte's double-rAF `shown` flip never ran → modals were present+clickable but `opacity:0`/`scale(.9)` (no `.show`). This hit ALL Modal.svelte modals (changelog too), NOT just FilterModal — a compositor artifact, not a bug. A `browser_resize` woke rAF and `.show`/opacity:1 applied immediately. LESSON: if a migrated modal renders but is invisible under Playwright, check `requestAnimationFrame` is firing (resize to wake it) before suspecting the component.
- **11G** ✅ teardown. By this point NO modal extended `BaseModal` and NOTHING called any `ModalService` method except the tutorial's `hideAllModals()` — which was already a NO-OP (its internal `modals` Map only ever held vanilla `showModal()`-injected modals, and there were none left). So the teardown was pure dead-code removal + one semantic-preserving swap. **DELETED `BaseModal.ts`, `ModalService.ts`** (+ their `ui/components/index.ts` & `services/ui/index.ts` exports; `ModalQueue.ts`/`AutoScheduleIntroModal.ts` were already gone in 11D). Added `UIStateManager.closeAllModals()` (`uiState.openModals = []`) as the declarative replacement for `hideAllModals()`; rewired the 3 tutorial call sites (setupTutorial ×2 in `onUIStateTransition`, TutorialStateMachine `restoreSnapshot`) to it — the actual modal closing was already done by the adjacent `modalClosed` loop / `restoreState` (which sets `openModals=[]`), so `closeAllModals()` is now strictly MORE correct (it truly clears the rune) yet behavior-identical. Removed `services.modalService` (ServiceContainer field + AppBootstrap construction/`profileStateManager.setModalService` call), `ProfileStateManager.setModalService` (was a no-op stub) + its `ModalService` import, `MainController.getModalService()` (zero callers) + the `modalService` ctor destructure/`scheduleController.setModalService` wiring + import, and `ScheduleController`'s `modalService` field/`setModalService`/import — its two remaining `this.modalService` truthiness GUARDS (delete-local-event click + `openAutoSchedule` bail-out) became `this.uiStateManager` (set at the same construction point, so identical semantics). Trimmed `src/types/modal.ts` to just the still-used `SectionData` (dropped now-dead `IModal`/`ModalOptions`/`ModalType`/`ModalEvent*` + their `types/index.ts` re-export). Fixed the now-stale ModalLayer header comment (it claimed vanilla modals still render via ModalService). tsc clean, build clean, 6/6 tests. **Re-ran the FULL tutorial under Playwright** (fresh localStorage, all 4 sub-tutorials end-to-end): course-selection → filtering (opens `filter-modal`) → auto-scheduling (opens auto-schedule-intro→`auto-schedule-filter`, exercising ScheduleController's swapped `uiStateManager` guard in `openAutoSchedule`) → schedule-manager (opens `schedule-picker`/Settings/Export). **Verified the `closeAllModals` path directly**: Back across the filter-modal boundary ("Filter by term" → "Open filters") closed the modal (dialog count 1→0 via `restoreSnapshot`→`closeAllModals`), Next reopened it (0→1); Back WITHIN the modal kept it open (snapshot reopens). 0 app console errors across the whole 85-message run. (The very last step "Almost there!" is `waitFor:'manual'` and asks the user to actually create+name a schedule via a native `prompt()` — pre-existing design, not driven here; `onComplete`→`cleanupTutorial` references nothing removed. The rAF-freeze gotcha from 11F recurred — resize to wake it.)

## PHASE 12 — ScheduleController (biggest) → decomposed, grid last  (12A ✅ + 12B ✅ + 12C+D ✅)
Sub-phases, each committable. Mount into existing schedule-page hosts.
- **12A** ✅ `AutoScheduleControls.svelte` (footer) — leaf; mounts into `.schedule-sidebar-content-footer` (host kept for the `~ .schedule-sidebar-content-footer` wizard-active CSS sibling rule). Renders the Auto-Schedule button (WAND icon + `#auto-schedule-btn` id preserved for the tutorial selector) OR the prev/restart/next nav + progress bar, gated on `appState.autoScheduleGeneration` — a NEW rune the orchestrator bumps in `notifyStateChange()` (called on generate success/empty/error, navigate, reset, and the selection-invalidation watch). Deleted `AutoScheduleOrchestrator.onStateChange`/`onStateChangeCallback`, `ScheduleController.setupAutoScheduleButton()` + `updateAutoScheduleButtonUI()` (and its calls in `doGenerateSchedules`), the constructor subscription, and the static footer markup in index.html. Nav handlers call `orchestrator.navigateSchedule(±1)` then `onAfterNavigate()` → `ScheduleController.renderScheduleGrids()` (grid still vanilla until 12C/D); button/restart → `onOpenAutoSchedule()` → `openAutoSchedule()`. Playwright-verified: real worker generation (2 schedules) flips footer button→nav+progress (bar 50%); Next→100%/Prev→50%/wrap-around; Restart opens `auto-schedule-intro`; clearing selection resets footer→button; 0 console errors. The generating-overlay spinner stays in `doGenerateSchedules` (grid concern → Phase 12C/D).
- **12B** ✅ `ScheduleSidebar.svelte` + `SelectedCourseItem.svelte` + `CalendarEventsButton.svelte`. ScheduleSidebar mounts into a NEW `display:contents` child wrapper `#schedule-sidebar-courses` inside `#schedule-sidebar-content`, so the vanilla `ComponentSelectionWizard` panel (appendChild'd to the parent by `BaseSidebarPanel.open()`) survives as a sibling. Reactive on `appState.selectedCourses` — term grouping/separators + per-course `SelectedCourseItem` (code/name/selected-components/incomplete-warning/credits + clear-sections/remove). `getIncompleteSelectionInfo` (made public) + `hasValidTimeSlot` kept on ScheduleController and passed as a prop; CalendarEventsButton → `openCalendarEventsPanel()`. Grid→sidebar hover highlight now goes through a `scheduleSidebarState.hoveredCourseId` rune (new `scheduleSidebarState.svelte.ts`) that `SelectedCourseItem` reads via `class:sidebar-course-highlighted` — replaced the `sidebarCourseItems` map. **Deleted** from ScheduleController: `displayScheduleSelectedCourses`, `renderCalendarEventsHeader`, `buildCourseHeaderHTML`, `buildAllCoursesHTML`, `buildCalendarEventsButtonHTML`, `getCourseFromElement`, `sidebarCourseItems`, and every `displayScheduleSelectedCourses()` call (5 internal + 4 in MainController); plus MainController's global `.course-remove-btn`/`.course-clear-sections-btn`/`.schedule-course-header` click delegation and the dead `updateSectionButtonStates` refresh loop. Playwright-verified: empty state, term separators + items, section-select shows `Lec B01`/clears warning/regroups to B Term, header click opens wizard WITH items+wrapper surviving, clear-sections + remove reactive, grid `.section-block` mouseenter toggles the sidebar highlight (false→true→false), calendar button opens the local-event modal; 0 console errors.
- **12C+D** ✅ `src/svelte/schedule/ScheduleGrids.svelte` + `TermGrid.svelte` + pure `scheduleGeometry.ts` + `schedulePreviewState.svelte.ts`. Replaced the imperative innerHTML renderer (`renderScheduleGrids`/`renderAffectedTerms`/`renderPopulatedGrid`/`buildCellOccupancyMap`/`getCellFromMap`/`updateTermHeaderConflictIndicator`/`addSectionBlockEventListeners`/`precomputeConflicts`/`applyPreviewOverlay`/`buildHoverCourse`/`generateCacheKey` + the per-cell `cellContentCache`/`conflictMap` fields + `setupTermFocusHandlers`/`focusTerm`/`unfocusTerm` + all 13 imperative call sites). **Geometry model changed** from per-cell+`isFirstSlot` to an **absolute-overlay**: TermGrid renders the static scaffold (term label + day headers + 60 background cells) + one `position:absolute; inset:0` `.block-layer` (grid-area = the 5-day×12-hour body; `.schedule-grid` made `position:relative`); each block is positioned by % `top=(startMin-480)/720`, `height=dur/720`. **Side-by-side column packing** (greedy `packColumns`) so overlapping classes/events render at 1/N width instead of stacking/hiding; conflict overlays are per-pair (deduped by region) and span the full day column. Preview = shared `schedulePreviewState` rune (previewCourse/selections/hover) the still-vanilla wizard writes + `showSectionInfoModal` reads. Color reactivity via new `appState.colorGeneration` (bumped in `CourseColorService.setCourseColor`); generating overlay via new `appState.scheduleGenerating` (`doGenerateSchedules` toggles it, `{#if}` renders `.schedule-generating-overlay`). Term focus = local `$state` + `class:focused`/`class:focused-term` + window Escape; desktop-only (`is-mobile` guard). MainController mounts ScheduleGrids into a `display:contents` `#schedule-grids-root` in `.schedule-main`; dropped `onAfterNavigate` (navigate updates `selectedCourses` → grid reacts). Playwright-verified: block geometry (rows=13, body-aligned, 2pm→correct top/height), hover→sidebar highlight, section-click→section-info modal, term focus + Escape, per-pair conflict overlays (multi-cluster days), calendar events + delete modal, wizard preview (dashed), recolor, side-by-side 2-way/3-way/lone; tsc 0 / build clean / 6-6 tests / 0 console errors.
- **12E** (multi-unit — do NOT delete the whole controller in one pass):
  - **12E.1** ✅ standalone `CalendarEventProvider`. New `src/services/scheduling/calendarEventProvider.ts` — a plain object reading `appState.activeSchedule?.localEvents` directly (no reactivity needed; on-demand getters called during generation). Ported `getAllLocalEventBlockedTimes`/`getLocalEventCount` verbatim (visible-only, skip `EventType.ONE_TIME`, expand `days × terms` defaulting `[AcademicTerm.ALL]`, exact `${id}-${term}-${day}` block id). Wired into `MainController` (`setCalendarEventProvider(calendarEventProvider)` replacing `this.scheduleController`). **Deleted** from ScheduleController: `implements CalendarEventProvider` + the import, `getAllLocalEventBlockedTimes`/`getLocalEventCount`/`getAllCalendarBlockedTimes`, and the confirmed-dead `handleSectionSelection`/`updateSectionButtonStates` (zero callers), plus the now-unused `AcademicTerm`/`EventType` imports. The orchestrator keeps its own `getAllCalendarBlockedTimes`/`getLocalEventCount` delegators (FilterModalController calls those via the orchestrator — now route through the standalone provider). `currentSchedule` stays for the remaining controller responsibilities. Playwright-verified: orchestrator getters return correct count (2: visible recurring + one-time; hidden excluded) + 4 blocked slots (recurring M/W × A/B; one-time + hidden skipped) with exact id template; grid still renders `external-event-block`s reactively from the same rune (M/W in A term, correct geometry) after `updateSchedule`; tsc 0 / build clean / 6-6 tests / 0 console errors.
  - **12E.2** ✅ clear-all-sections button → declarative `src/svelte/ClearAllSectionsButton.svelte`. Mounted into a `#clear-all-sections-slot` (`display:contents`) in `.schedule-filter-controls` (CalendarEventsButton pattern); renders its own `<button id="clear-all-sections-btn">` (same id/classes/title/aria-label + eraser icon), takes `courseSelectionService` as a prop, behavior identical (empty / no-sections alerts; confirm → `clearAllComponents()`). Deleted `setupClearAllSectionsButton`/`handleClearAllSections` + the now-unused `getInlineSVG` import from ScheduleController; MainController's setup call → `mount()`. Playwright-verified all three paths; tsc 0 / build clean / 6-6 / 0 console errors.
  - **12E.3** ✅ local-event CRUD → standalone `src/services/scheduling/localEventService.ts`. Reads `appState.activeSchedule` directly (no `currentSchedule`) and persists add/delete via `profileStateManager.updateSchedule(id, { localEvents }, 'calendar-event-exclusion')` so the grid re-renders reactively. Injected `init(profileStateManager, uiStateManager)` from MainController (PSM isn't a singleton). Rewired CalendarEventsButton `onClick` → `openAddModal()` and grid `onOpenDeleteEvent` → `openDeleteModal(eventId)`. Deleted from ScheduleController: the 5 CRUD methods + `currentSchedule`/`onScheduleUpdate` fields + `loadExternalEvents` + `setScheduleUpdateCallback` + `Schedule`/`LocalCalendarEvent` imports; removed both dead `loadExternalEvents()` call sites + the `setScheduleUpdateCallback` wiring in MainController. Playwright-verified add+delete end-to-end (persist + grid 8↔0 blocks + auto-scheduler getters); tsc 0 / build clean / 6-6 / 0 console errors.
  - **12E.4** ✅ section-info modal (+ color get/set) → standalone `src/services/scheduling/sectionInfoService.ts`. `show(courseId, sectionNumber)` resolves the clicked section from either the wizard preview rune (`schedulePreviewState`) or the saved selection (`CourseSelectionService`), then sets `modalState.sectionInfo` + `modalOpened('section-info')`. Color get/set route straight through `CourseColorService` — this absorbs ScheduleController's private `getCourseColor`/`setCourseColor` delegators, which had no other callers (`ScheduleGrids.svelte` already uses `colorService.getCourseColor` directly). Injected `init(courseSelectionService, colorService, uiStateManager)` from MainController; rewired the grid's `onOpenSectionInfo` → `sectionInfoService.show(...)`. Deleted from ScheduleController: `showSectionInfoModal`, `getCourseColor`, `setCourseColor` (the `colorService` field stays — still used by the auto-schedule intro `getColor`). Playwright-verified: clicking a section block opens the modal with correct professor/section/CRN/credits/color/meeting-times; changing the color input patches `appState.selectedCourses` `customColor` (grid re-colors); tsc 0 / build clean / 6-6 / 0 console errors.
  - **12E.5** ✅ schedule filter button → declarative `src/svelte/ScheduleFilterButton.svelte`. Mirrors the planner `#filter-btn` in FilterButtons.svelte: renders its own `<button id="schedule-filter-btn" class="btn btn-secondary filter-btn">` (FILTER_FILLED icon) with `class:active` + title `$derived` from `(filterService.getActiveFilters(), filterService.hasNonDefaultFilters(appState.activeSchedule?.year))` + `getFilterCount()`, `onFilter` prop → `openFilterModal()`. Mounted into a `#schedule-filter-slot` (`display:contents`) in `.schedule-filter-controls`. **Deleted** the entire imperative filter-button refresh plumbing it replaces: the icon-insert + click wiring in MainController, BOTH `updateScheduleFilterButtonState` copies (MainController's richer `hasNonDefaultFilters(activeYear)` one + ScheduleController's older `isEmpty()` one), `ScheduleController.applyFiltersAndRefresh`, the `watch` in `ScheduleController.setFilterService` + the `watch` import, MainController's filter `watch`, and the `setTimeout(100)` initializer. `MainController.refreshCurrentView` is now a documented no-op (its only job was that refresh; its callers — ViewToggle.onSelect / onDataRefreshed / refreshUI / initializeDefaultDepartmentView — stay intact for a later imperative view-refresh teardown). `setFilterService` keeps its conflictDetector wiring (still sets `this.filterService` for openAutoSchedule). Playwright-verified: button renders in the slot (icon + classes + title), click opens the FilterModal, adding/removing a non-default filter flips `active`/title reactively with no watch; tsc 0 / build clean / 6-6 / 0 console errors.
  - **12E.6** ✅ auto-schedule modal orchestration + `doGenerateSchedules` → standalone `src/services/scheduling/autoScheduleService.ts`. Holds the intro → filter → generate sequence (declarative `modalState` + continuation callbacks) and generation via `AutoScheduleOrchestrator`, surfacing the generating overlay through the `appState.scheduleGenerating` rune. Injected `init(courseSelectionService, filterService, colorService, orchestrator, uiStateManager)` from MainController (none are module singletons). Rewired the grid mount `onOpenAutoSchedule` + MainController's 6 wrapper methods (`openAutoSchedule`/`openAutoScheduleIntro`/`openAutoScheduleFilter`/`updateAutoScheduleIntroTerms`/`refreshAutoScheduleFilterUI`/`runAutoSchedule`, still called by the tutorial) to delegate to the service. **Deleted** from ScheduleController: all 8 auto-schedule methods (`openAutoSchedule`, `openScheduleFilterModal`, `openAutoScheduleIntro`, `updateAutoScheduleIntroTerms`, `openAutoScheduleFilter`, `refreshAutoScheduleFilterUI`, `runAutoSchedule`, `doGenerateSchedules`) plus the now-dead `colorService`/`autoScheduleOrchestrator` fields + their constructor params (constructor is now just `courseSelectionService`) and the now-unused `appState`/`modalState`/`WeeklyTimeSlot`/`AutoScheduleOrchestrator`/`CourseColorService` imports; updated the `new ScheduleController(...)` call in MainController. ScheduleController now owns ONLY the wizard methods + `getIncompleteSelectionInfo`/`hasValidTimeSlot`. Playwright-verified the full flow through the service (intro modal with both courses → Next → filter modal → Generate → orchestrator applied lectures A01/B01 + modal closed); profile reverted; tsc 0 / build clean / 6-6 / 0 console errors.
  - **12E.next** (remaining unit before the controller can be deleted): relocate the **wizard methods** (`openComponentWizard`/`jumpWizardToStep`/`closeComponentWizard`/`onWizardComplete`/`onWizardSelectionChange`/`onWizardHoverPreview`) + `getIncompleteSelectionInfo`/`hasValidTimeSlot` off `ScheduleController`. THEN delete `ScheduleController.ts` + index export; finish the imperative view-refresh teardown (`refreshCurrentView` no-op + its callers).

## PHASE 13 — `App.svelte` root shell (LAST)
Collapse `index.html` to `<div id="app">` + `mount(App)`. Absorb page/view DOM effects → `{#if}`/`class:`. Retire `UIStateManager` DOM-effect role. Convert settings mobile menu here. Then delete `RUNES_MIGRATION*.md` + `reactivity.svelte.ts` once the last `watch`/`subscribe` bridge consumer is gone.

---

## Per-unit loop (every unit)
1. (planning already done above; agent re-reads the relevant source + `DepartmentSidebar.svelte`)
2. Agent writes the component(s) + rewires MainController/etc. + deletes vanilla code.
3. `bunx tsc --noEmit` (no new errors) + `bun run build` (clean) + `bun run test`.
4. Playwright: dev server on `http://localhost:3000/wpiplannerV2/`, drive the feature, confirm behavior + 0 console errors.
5. Commit. Update this plan + `RUNES_MIGRATION.md`.
