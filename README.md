## TODO:
0. Finish fixing reload functioanly and auto detect changes.
1. Fix filtering system, specifically with conflicts. Only apply section filters ever and then for course filtering just wrap the section filter to show courses that have sections left after filters.
2. Add cloud sync.
3. Add oscar rating system in alongside the RMP system. Integrate it nicely.

Add a autofill suggestion menu for the other searches.
Add the clear all filter button next to the filters buttons
Fix the filtering for searches so section code's and couse codes when searched will show up first.
Change the sytling on the suggestion menu for seaches